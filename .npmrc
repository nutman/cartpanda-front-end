# Skip optional dependencies during install to avoid native module build failures
# canvas is only needed for jsdom in tests, not for production builds
# npm will skip optional dependencies that fail to build, but we can also
# configure Vercel to omit them entirely during install
